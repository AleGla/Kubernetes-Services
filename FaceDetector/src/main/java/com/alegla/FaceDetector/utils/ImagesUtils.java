package com.alegla.FaceDetector.utils;

//import java.awt.Graphics2D;
//import java.awt.image.BufferedImage;
//import java.util.Base64;

//import javax.imageio.ImageIO;

public class ImagesUtils {

//	public static refineImage(String images) {
//		ByteArrayDataSource attachment = null;
//		attachment = new ByteArrayDataSource(Base64.Decoder(images), "image/jpeg", "image");
//		BufferedImage inputImage;
	//		try {
	//			inputImage = ImageIO.read(attachment.getInputStream());
	//		BufferedImage outputImage = new BufferedImage(inputImage.getWidth(), inputImage.getHeight(), inputImage.getType());
	//		Graphics2D g2d = outputImage.createGraphics();
	//		g2d.drawImage(inputImage, 0, 0, inputImage.getWidth(), inputImage.getHeight(), null);
	//		g2d.dispose();
	//		final ByteArrayOutputStream imagebyte = new ByteArrayOutputStream();
	//		ImageIO.write(outputImage, "jpg", imagebyte);
	//		String base = new String(Base64.encodeBytes(imageByte.toByteArray()));
	//		return base;
	//	}catch(IOException e) {
	//		return null;
	//	}
	//	}
}
